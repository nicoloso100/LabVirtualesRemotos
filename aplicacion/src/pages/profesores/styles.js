import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  FiltroContenedor: {
    marginBottom: "20px",
  },
  button: {
    width: "100%",
    maxWidth: "300px",
    marginLeft: "40px",
  },
}));
