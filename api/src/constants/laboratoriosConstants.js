const LaboratoriosConstants = param => {
  return {
    labsError:
      "No se ha podido obtener la información del laboratorio, contacte con un administrador"
  };
};

module.exports = LaboratoriosConstants;
