const LaboratoriosCursosConstants = param => {
  return {
    addLabCursoError:
      "Ha ocurrido un error al realizar la asociación de los laboratorios, por favor comuníquese con un administrador"
  };
};

module.exports = LaboratoriosCursosConstants;
